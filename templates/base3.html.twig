<!--DOCTYPE html -->
<html>
<head>
    <title>
        {% block title %}
            Welcome!

        {% endblock %}
    </title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <link href="{{ asset('css/toto.css') }}" type="text/css" rel="stylesheet">
    <script src="{{ asset('js/toto.js') }}"></script>
    <link rel="stylesheet" href="//code.jquery.com/ui/1.13.1/themes/base/jquery-ui.css">
    <link rel="stylesheet" href="/resources/demos/style.css">
    <script src="https://code.jquery.com/jquery-3.6.0.js"></script>
    <script src="https://code.jquery.com/ui/1.13.1/jquery-ui.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <link rel="stylesheet" href="//code.jquery.com/ui/1.13.1/themes/base/jquery-ui.css">
    <script src="https://code.jquery.com/ui/1.13.1/jquery-ui.js"></script>
    <link rel="stylesheet" href="https://unpkg.com/bootstrap-table@1.20.0/dist/bootstrap-table.min.css">

    <!-- Latest compiled and minified JavaScript -->
    <script src="https://unpkg.com/bootstrap-table@1.20.0/dist/bootstrap-table.min.js"></script>
    <!-- Latest compiled and minified Locales -->
    <script src="https://unpkg.com/bootstrap-table@1.20.0/dist/locale/bootstrap-table-zh-CN.min.js"></script>

    <script>
        jQuery(document).ready(function() {
            $('.js-datepicker').datepicker({
                minDate: 0
            });
        });
    </script>
</head>
            {% if is_granted('ROLE_SECRETAIRE') %}
            {% endif %}
            {% if is_granted('ROLE_TRESORIER') %}
            {% endif %}
            {% if is_granted('ROLE_PRESIDENT') %}
                    <a href="#finance" data-toggle="collapse" aria-expanded="false" class="dropdown-toggle" style="color: #0000">
                        G
                    </a>
                            <a href="#" style="color: #0000">
                                S
                            </a>
                            <a href="#" style="color: #0000">
                                E
                            </a>
                            <a href="#" style="color: #0000">
                                S
                            </a>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}

                <li style="position:absolute; right:1500px;">
                    <a href="{{ path('app_logout') }}" style="color: #0000">
                        <i class="fa-thin fa-right-from-bracket"></i>Déconnexion
                    </a>
                </li>
                <li style="position:absolute; right:1500px;">
                    <a href="{{ path('amis_edit') }}" style="color: #0000">
                        Mon Profil
                    </a>
                </li>

            {% else %}
                <li>
                    <a href="{{ path('app_login') }}" style="color: #0000">
                        Se connecter
                    </a>
                </li>
            {% endif %}
        </ul>

        <ul class="list-unstyled CTAs"></ul>

        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">

                    <i class="fas fa-align-left"></i>

                <ul class="nav navbar-nav navbar-right">
                    <B style="color: #0000">
                        {% if app.user %}

                            {{ app.user.prenom }} {{ app.user.nom }} ({{app.user.roles[0] }})
                        {% endif %}
                    </B>
                </ul>
            </div>
        </nav>
    </div>
{% block content %}{% endblock content %}
</html>
